##### DATASOURCE #####
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:25432}/${DB_NAME:app}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:root}